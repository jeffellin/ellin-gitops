# AWS Credentials - secrets only
# Copy this file to aws-credentials and fill in your values
# Non-secret cluster config lives in clusters/<cluster-name>/cluster-vars.env

AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
AWS_REGION=us-east-1
